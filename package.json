{
  "name": "ember-service-worker-deferred-requests",
  "version": "0.0.0",
  "description": "An Ember Service Worker plugin that enqueue PUT, POST and PATCH requests while in offline in a buffer to perform the operations once the connection is regained.",
  "keywords": [
    "ember-addon",
    "ember-service-worker-plugin"
  ],
  "license": "MIT",
  "author": "Guillaume Gérard <guillaume.gerard88@gmail.com>",
  "repository": "",
  "scripts": {
    "test": "eslint ./*.js addon addon-test-support app config lib server test-support tests"
  },
  "dependencies": {
    "broccoli-merge-trees": "^2.0.0",
    "broccoli-plugin": "^1.3.0",
    "ember-service-worker": "^0.6.9"
  },
  "devDependencies": {
    "eslint": "^4.16.0",
    "eslint-plugin-ember": "^5.0.0",
    "eslint-plugin-node": "^5.2.1"
  },
  "engines": {
    "node": "^4.5 || 6.* || >= 7.*"
  }
}
